<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      System launcher identity uses the same Owner-approved studio master as auth/splash.
      Keep the artwork itself immutable; this wrapper only provides adaptive-icon safe-zone sizing.
    -->
    <item
        android:width="76dp"
        android:height="76dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/sentinel_master_icon"
            android:gravity="fill" />
    </item>
</layer-list>
