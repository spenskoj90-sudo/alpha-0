# Companion Session Composition v1

Provides one deterministic construction seam joining the existing runtime builder to `CompanionTransportSession`. Optional peer authentication is injected explicitly. No network connection is opened and no authorization semantics are weakened.
