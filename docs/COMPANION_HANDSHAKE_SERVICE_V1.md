# Companion Handshake Service v1

Wraps the canonical exact-v1 `negotiate_handshake` function with an explicit compatibility configuration. Version and capability mismatches fail closed with the canonical reason codes. No transport I/O is performed.
