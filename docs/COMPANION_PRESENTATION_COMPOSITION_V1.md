# Companion Presentation Composition v1

Maps bounded Companion health and recommendation API results to the existing presentation-only `CompanionPresentation` contract.

The bridge preserves confidence and provenance, emits only bounded text, and cannot authorize or execute actions. Overlay and voice are presentation channels, not action channels.
